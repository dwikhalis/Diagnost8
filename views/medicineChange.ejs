<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("./navbar/headnavbar.ejs"); %>
    <title>Diagnost8</title>
</head>
<body>
    <%- include("./navbar/bodynavbar.ejs"); %>
    <%- include("./navbar/adminnavbar.ejs"); %>
    <div class="container">
        <div class="mt-4 ml-5 mr-5 mb-4">
            <h1>Medicine Change</h1>
            <form action="/medicines/<%= data[0].id %>/change" method="post">
                <label for="name">Name</label><br>
                <input class="form-control" type="text" name="name" value="<%= data[0].name %>" required>
                <br><br>
                <label for="company">Company</label><br>
                <input class="form-control" type="text" name="company" value="<%= data[0].company %>" required>
                <br><br>
                <label for="regDate">Registration Date</label><br>
                <input class="form-control" type="date" name="regDate"
                    value="<%=data[0].regDate.toLocaleDateString('id',{year:'numeric',month:'2-digit',day:'2-digit'}).split('/')[2] + "-" +
                    data[0].regDate.toLocaleDateString('id',{year:'numeric',month:'2-digit',day:'2-digit'}).split('/')[1] + "-" +
                    data[0].regDate.toLocaleDateString('id',{year:'numeric',month:'2-digit',day:'2-digit'}).split('/')[0]%>"
                required>
                <br><br>
                <label for="description">Description</label><br>
                <textarea class="form-control" name="description" required><%= data[0].description %></textarea>
                <br><br>
                <input class="form-control" type="submit" value="Change Medicine">
            </form>
</div>
</div>
</body>
</html>